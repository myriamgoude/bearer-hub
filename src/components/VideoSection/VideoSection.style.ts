import { css } from '@emotion/core'

export default {
  root: css`
    display: flex;
    align-items: center;
  `,
  button: css`
    position: relative;
    z-index: 10;
    margin-top: -1rem;
  `
}
